# Navanchauhan Tap

## How do I install these formulae?

`brew install navanchauhan/tap/<formula>`

Or `brew tap navanchauhan/tap` and then `brew install <formula>`.

## Documentation

`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
